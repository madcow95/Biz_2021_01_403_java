package com.callor.book;

public class Page_74 {
	
	public static void main(String args[]) {
		
		double mathScore = 90.45;
		double engScore = 75.12;
		double totalScore = mathScore + engScore;
		
		System.out.println(totalScore);
		
		double avrScore = totalScore / 2.0;
		System.out.println(avrScore);
		
		
		
		// page 78
		// int num = 15;
		// if(num > 10 && num < 2)
		// && 문은 둘다 참일때만 값이 true로 나타난다.
		// 둘 중 하나라도 거짓일 때는 false로 나타난다.
		// 둘다 거짓일 때, 앞의 것인 num > 10이 거짓이면 거기서 break;된다.
		
		// || 문은 둘다 거짓일 때만 값이 false로 나타난다.
		// 둘 중 하나라도 참일 때는 true로 나타난다.
		// 둘 중 하나라도 참일 때 앞의 것인 num > 10만 판단하고 뒤의것인 num < 2는 참, 거짓 상관없이 
		// 앞에서 break; 가 된다.
				
		
		
	}

}
